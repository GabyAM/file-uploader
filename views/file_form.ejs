<%- include('modal_form.ejs', 
{
  name: locals.name,
  action: locals.action, 
  enctype: 'multipart/form-data',
  title: locals.title,
  formInputs: [
    {type: 'file', name: 'file', label: 'Select a file'},
    {type: 'text', name: 'name', label: 'Name'},
    {type: 'select', options: locals.folders, name: 'folder', label: 'Select a folder'}
  ], 
  errors: locals.errors || null,
  values: locals.values || null
}) %>
